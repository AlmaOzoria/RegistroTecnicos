@page "/Cliente/Delete"
@page "/Cliente/Delete/{ClientesId:int}"
@inject ClientesServices clientesServices
@inject NavigationManager navigationManager
@inject NotificationService NotificationService
@rendermode InteractiveServer

<PageTitle>Eliminar Clientes</PageTitle>
@if (Clientes != null)
{
    <div class="container">
        <div class="card shadow-header">

            <div class="card-header  bg-primary text-white text-center py-2 rounded">
                <h3><strong>Eliminar Clientes</strong></h3>
            </div>
            <div class="card-header">
                <h3 class="text-danger">¿Estás seguro de que quieres eliminar el siguiente cliente?</h3>
            </div>

            <div class="container">
                <div class="row">
                    <div class="col-md-6">
                        <div class="card shadow-lg">
                            <div class="card-header">
                                <h3 class="text-center"><strong>Datos del cliente</strong></h3>
                            </div>
                            <div class="card-body">
                                <div class="border-primary border-2 borde">
                                    <div class="card-body">
                                        <div class="row">
                                            <div class="col-md-8">
                                                <ul class="list-unstyled">
                                                    <li><strong>ClienteId:</strong> @Clientes.ClientesId</li>
                                                    <li><strong>Nombre:</strong> @Clientes.Nombres</li>
                                                    <li><strong>WhatsApp:</strong> @Clientes.WhatsApp</li>
                                                </ul>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card-footer">
                @*Boton Eliminar*@
                <button type="button" class="btn btn-outline-danger" @onclick="EliminarTecnico"><i class="bi bi-trash-fill"></i> Eliminar</button>
                @*Boton Volver*@
                <button type="button" class="btn btn-outline-primary" @onclick="Volver"><i class="bi bi-box-arrow-left"></i> Volver</button>
            </div>

        </div>
    </div>
}
else
{
    <p>Cliente no encontrado.</p>
}

@code {
    [Parameter]
    public int ClientesId { get; set; }
    public Clientes? Clientes { get; set; } = new Clientes();

    protected override async Task OnInitializedAsync()
    {
        if (ClientesId != 0)
        {
            Clientes = await clientesServices.Buscar(ClientesId);
        }
    }

    public async Task EliminarTecnico()
    {
        var eliminado = await clientesServices.Eliminar(Clientes.ClientesId);
        Volver();
    }

    public void Volver()
    {
        navigationManager.NavigateTo("/Cliente/Index");
    }
}
